# Manually trigger a single frame acquisition.
# The node must be in the active lifecycle state.

bool wait_grabbing_end  # If true, the call blocks until frame grabbing completes on the device (before transfer and publishing).
---
bool success    # True if the frame was triggered successfully.
string message  # Error description if success is false.
